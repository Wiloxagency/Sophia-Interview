// import { GptFormData } from "../../types";
// import { TASK_FIELDS } from "../handleSendMessage/taskTypes";

// export function getNextIncompleteTask(
//   tasks: GptFormData["tasks"]
// ): string | null {
//   for (const [key, task] of Object.entries(tasks)) {
//     if (TASK_FIELDS.some((field) => task[field] == null)) {
//       return key;
//     }
//   }
//   return null;
// }
